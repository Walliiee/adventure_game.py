Standalone scripts and demos — not part of Wildlands. Run from repo root, e.g.:

```bash
python examples/main.py
python examples/slot_machine.py
```

Some scripts depend on others in this folder (e.g. `main.py` uses `bday_message.py`).
